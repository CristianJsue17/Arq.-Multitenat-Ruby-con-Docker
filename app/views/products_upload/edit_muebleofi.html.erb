<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Editar Producto</title>
  <style>
    /* Estilos modernos para el formulario de edición de productos */
    body {
      background-color: #f8f2e9; /* Color beige característico */
      font-family: 'Roboto', sans-serif;
      display: flex;
      justify-content: center;
      align-items: flex-start;
      padding: 40px;
      color: #333;
    }

    .container {
      width: 100%;
      max-width: 700px;
      position: relative;
    }

    h1 {
      text-align: left;
      color: #5c4b3c; /* Color marrón oscuro para contrastar */
      font-size: 28px;
      margin-bottom: 20px;
      font-weight: bold;
      padding-left: 10px;
      border-left: 4px solid #d2b48c; /* Beige oscuro */
      margin-left: 10px;
    }

    .back-link {
      position: absolute;
      top: 10px;
      right: 10px;
      font-size: 14px;
      color: #5c4b3c; /* Marrón oscuro */
      text-decoration: none;
      font-weight: bold;
      padding: 8px 12px;
      border: 1px solid #5c4b3c;
      border-radius: 5px;
      transition: background-color 0.3s ease, color 0.3s ease;
    }

    .back-link:hover {
      background-color: #5c4b3c;
      color: #ffffff;
    }

    .product-form {
      width: 100%;
      background: #fffaf0; /* Beige claro */
      padding: 30px 35px;
      border-radius: 12px;
      box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1);
      display: flex;
      flex-direction: column;
      gap: 20px;
    }

    .form-group {
      display: flex;
      flex-direction: column;
      gap: 8px;
    }

    .form-group label {
      font-weight: 500;
      color: #5c4b3c; /* Marrón oscuro */
    }

    .input-field {
      width: 100%;
      padding: 12px 15px;
      border-radius: 8px;
      border: 1px solid #ccc;
      font-size: 15px;
      background-color: #faf4e6; /* Beige muy claro */
      transition: border-color 0.3s ease, box-shadow 0.3s ease;
    }

    .input-field:focus {
      border-color: #d2b48c; /* Beige oscuro */
      box-shadow: 0 0 5px rgba(210, 180, 140, 0.3);
      outline: none;
      background-color: #fff;
    }

    .submit-button {
      width: 100%;
      padding: 15px;
      background-color: #d2b48c; /* Beige oscuro */
      color: #fff;
      border: none;
      border-radius: 8px;
      font-size: 18px;
      cursor: pointer;
      font-weight: bold;
      transition: background-color 0.3s ease;
      box-shadow: 0 4px 10px rgba(210, 180, 140, 0.2);
    }

    .submit-button:hover {
      background-color: #a37c4d; /* Beige más oscuro */
    }

    /* Estilo de las secciones divididas */
    .form-row {
      display: flex;
      flex-wrap: wrap;
      gap: 15px;
    }

    .form-column {
      flex: 1;
      min-width: 250px;
    }

    /* Estilo para el selector de archivos */
    .input-file {
      padding: 10px 12px;
      border: 1px solid #ddd;
      border-radius: 8px;
      font-size: 15px;
      background-color: #faf4e6; /* Beige muy claro */
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    .input-file:hover {
      background-color: #e6ddca; /* Beige intermedio */
    }

    /* Estilo para la pantalla pequeña */
    @media (max-width: 768px) {
      .product-form {
        padding: 25px;
      }

      .form-row {
        flex-direction: column;
      }
    }
  </style>
</head>
<body>

  <div class="container">
    <a href="<%= new_products_upload_path %>" class="back-link">← Volver a Productos</a>
    <h1>Editar Producto</h1>

    <%= form_with(model: @product, url: update_products_upload_path(@product), method: :patch, local: true, class: "product-form") do |f| %>
      <div class="form-row">
        <div class="form-column">
          <div class="form-group">
            <%= f.label :name, 'Nombre del Producto' %>
            <%= f.text_field :name, class: "input-field", placeholder: "Ingrese el nombre del producto", required: true %>
          </div>

          <div class="form-group">
            <%= f.label :description, 'Descripción' %>
            <%= f.text_area :description, class: "input-field", placeholder: "Ingrese una descripción", required: true %>
          </div>

          <div class="form-group">
            <%= f.label :price, 'Precio' %>
            <%= f.number_field :price, step: 0.01, class: "input-field", placeholder: "Ingrese el precio", required: true %>
          </div>

          <div class="form-group">
            <%= f.label :category, 'Categoría' %>
            <%= f.select :category, options_for_select(['escritorios', 'sillas', 'armarios'], @product.category), class: "input-field" %>
          </div>

          <div class="form-group">
            <%= f.label :brand, 'Marca' %>
            <%= f.text_field :brand, class: "input-field", placeholder: "Ingrese la marca", required: true %>
          </div>
        </div>

        <div class="form-column">
          <div class="form-group">
            <%= f.label :stock, 'Stock' %>
            <%= f.number_field :stock, class: "input-field", placeholder: "Ingrese la cantidad en stock", required: true %>
          </div>

          <div class="form-group">
            <%= f.label :material, 'Material' %>
            <%= f.text_field :material, class: "input-field", placeholder: "Ingrese el material", required: true %>
          </div>

          <div class="form-group">
            <%= f.label :dimension, 'Dimensión' %>
            <%= f.text_field :dimension, class: "input-field", placeholder: "Ingrese las dimensiones", required: true %>
          </div>

          <div class="form-group">
            <%= f.label :peso, 'Peso' %>
            <%= f.text_field :peso, class: "input-field", placeholder: "Ingrese el peso", required: true %>
          </div>

          <div class="form-group">
            <%= f.label :color, 'Color' %>
            <%= f.text_field :color, class: "input-field", placeholder: "Ingrese el color", required: true %>
          </div>
        </div>
      </div>

      <div class="form-group">
        <%= f.label :caracteristicas, 'Características' %>
        <%= f.text_area :caracteristicas, class: "input-field", placeholder: "Ingrese las características", required: true %>
      </div>

      <div class="form-group">
        <%= f.label :image, 'Imagen del Producto' %>
        <%= f.file_field :image, class: "input-file" %>
      </div>

      <%= f.submit 'Actualizar Producto', class: "submit-button" %>
    <% end %>
  </div>

</body>
</html>
